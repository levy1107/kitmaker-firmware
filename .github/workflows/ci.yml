# .github/workflows/ci.yml

name: Build OTA Binary

on:
  push:
    paths:
      - 'sketches/**'
      - '.github/workflows/ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        with:
          persist-credentials: true

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v1

      - name: Install ESP32 core
        run: |
          arduino-cli core update-index
          arduino-cli core install esp32:esp32

      - name: Install required libraries
        run: |
          arduino-cli lib update-index
          arduino-cli lib install "Adafruit SSD1306"
          arduino-cli lib install "Adafruit GFX Library"
          arduino-cli lib install "Adafruit HTU21DF Library"
          arduino-cli lib install "Adafruit NeoPixel"

      - name: Compile all .ino in sketches/ into firmware/
        run: |
          mkdir -p firmware
          for sketch in sketches/*.ino; do
            echo "📦 Compiling $sketch"
            arduino-cli compile \
              --fqbn esp32:esp32:esp32 \
              "$sketch" \
              --output-dir firmware
          done

      - name: Publish latest.bin
        run: |
          # Renombramos el .bin recién compilado a latest.bin
          mv firmware/*.bin firmware/latest.bin
          git config user.name "github-actions[bot]"
          git config user.email "actions@github.com"
          git add firmware/latest.bin
          git commit -m "🔨 Build latest.bin"
          git push
