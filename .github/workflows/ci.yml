# .github/workflows/ci.yml

name: Build OTA Binary

on:
  push:
    paths:
      - 'sketches/**'
      - '.github/workflows/ci.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Setup Arduino CLI
      uses: arduino/setup-arduino-cli@v1

    - name: Install ESP32 core
      run: |
        arduino-cli core update-index
        arduino-cli core install esp32:esp32

    - name: Compile sketch
      run: |
        arduino-cli compile \
          --fqbn esp32:esp32:esp32 sketches/automatic.ino

    - name: Publish latest.bin
      run: |
        # 1) Localiza cualquier .bin dentro del build
        BIN=$(find sketches/automatic.ino/build -type f -name "*.bin" | head -n1)
        echo "📦 BIN encontrado en: $BIN"
        # 2) Asegura la carpeta firmware/
        mkdir -p firmware
        # 3) Copia y commitea
        cp "$BIN" firmware/latest.bin
        git config user.name "github-actions[bot]"
        git config user.email "actions@github.com"
        git add firmware/latest.bin
        git commit -m "🔨 Build latest.bin"
        git push
